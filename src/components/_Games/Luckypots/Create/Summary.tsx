import type { Luckypot } from "@/types";

const Summary = ({
  formData,
  formChange,
  handleSubmit,
}: {
  formData?: Luckypot | any;
  formChange?: any;
  handleSubmit?: any;
}) => {
  return <></>;
};

export default Summary;
